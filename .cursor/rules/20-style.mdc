---
description: "UI/UX and accessibility standards for React components (Tailwind, a11y, modern layout)."
globs:
  - "app/**/page.tsx"
  - "components/**/*.tsx"
alwaysApply: false
---

# UI / Styling Rules

## Styling
- Use TailwindCSS.
- Mobile-first, clean, future-forward layout.
- Prefer readable spacing over dense UI.

## Components
- Prefer small functional components.
- Extract a component only when reuse or clarity improves.

### Example (recommended pattern)
```tsx
import Image from "next/image";

export function Hero() {
  return (
    <section className="mx-auto max-w-6xl px-6 py-24 text-center">
      <Image
        src="/brand/logo.png"
        alt="Soul Games Studios logo"
        width={120}
        height={120}
        className="mx-auto mb-6"
        priority
      />

      <h1 className="text-4xl font-semibold tracking-tight md:text-5xl">
        Your AI board of agents — built for future founders
      </h1>

      <p className="mt-4 text-lg text-muted-foreground">
        Calm, strategic AI support for building sustainable companies.
      </p>
    </section>
  );
}
